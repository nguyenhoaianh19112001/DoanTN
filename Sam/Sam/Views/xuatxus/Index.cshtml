@model IEnumerable<Sam.Models.xuatxu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenxuatxu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenxuatxu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.maxuatxu }) |
            @Html.ActionLink("Details", "Details", new { id=item.maxuatxu }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.maxuatxu })
        </td>
    </tr>
}

</table>
