@model IEnumerable<Sam.Models.dungtich>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tendungtich)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tendungtich)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.madungtich }) |
            @Html.ActionLink("Details", "Details", new { id=item.madungtich }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.madungtich })
        </td>
    </tr>
}

</table>
