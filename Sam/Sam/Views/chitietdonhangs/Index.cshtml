@model IEnumerable<Sam.Models.chitietdonhang>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.donhang.diachi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sanpham.tensp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.soluong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dongia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.donhang.diachi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sanpham.tensp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.soluong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dongia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.machitietdh }) |
            @Html.ActionLink("Details", "Details", new { id=item.machitietdh }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.machitietdh })
        </td>
    </tr>
}

</table>
